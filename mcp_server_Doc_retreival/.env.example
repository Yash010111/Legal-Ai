# Example environment configuration for Legal Mind AI MCP server
# Copy this file to .env and fill in your token locally (DO NOT commit .env)

# Hugging Face Inference API token (leave empty if using local transformers)
HUGGINGFACE_API_TOKEN=

# Model repo id to use for Gemma 2 b (default used if unset in server)
GEMMA_MODEL=bigscience/gemma-2b
